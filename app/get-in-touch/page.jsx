import React from "react"
import GetInTouchComponents from "@/src/components/get-in-touch"
import { pageMetadata } from "@/src/config/seo"

export const metadata = pageMetadata("/get-in-touch")

const Page = () => {
  return <GetInTouchComponents />
}

export default Page
